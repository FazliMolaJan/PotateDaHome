<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        app:cardCornerRadius="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardBackgroundColor="?attr/bottom_bar_color"
        android:elevation="16dp"
        android:layout_margin="8dp"
        tools:context=".PotatoActivity">

    <LinearLayout
            android:onClick="startPotatoPicker"
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

        <TextView android:layout_width="match_parent"
                  android:layout_height="wrap_content"
                  android:text="@string/title_presets"
                  android:textColor="?attr/presets_text_color"
                  android:padding="16dp"
                  android:textAppearance="@android:style/TextAppearance.Material.Headline"/>

        <!-- COLORS OPTIONS -->
        <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/colors_rv"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingBottom="16dp"
                android:paddingStart="16dp"
                android:paddingEnd="16dp"
                tools:context=".MainActivity"/>
    </LinearLayout>

</com.google.android.material.card.MaterialCardView>