<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/defaultBackgroundColor</item>
        <item name="colorPrimaryDark">@color/defaultBackgroundColor</item>
        <item name="colorAccent">@color/defaultBackgroundColor</item>
    </style>
</resources>
